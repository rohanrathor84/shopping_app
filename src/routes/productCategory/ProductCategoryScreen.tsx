import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const ProductCategoryScreen = () => {
  return (
    <View>
      <Text>ProductCategoryScreen</Text>
    </View>
  );
};

export default ProductCategoryScreen;

const styles = StyleSheet.create({});
